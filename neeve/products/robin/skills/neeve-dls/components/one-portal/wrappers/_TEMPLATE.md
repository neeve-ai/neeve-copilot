# <ComponentName> (shared/components wrapper)

**Layer:** `shared/components` — wraps DLS `<DLSComponentName>`
(`components/dls/<file>.md`)

## When to use

<!-- what product need this wrapper exists for, and why the bare DLS component isn't
enough -->

## Props

| Prop | Type | Required | Default | Notes |
| ---- | ---- | -------- | ------- | ----- |

## Capabilities (confirmed features)

## Known limitations

<!-- the most important section — enumerate explicitly what this does NOT support -->

## If the request exceeds these limitations

Follow SKILL.md Step 2: ask whether to extend this wrapper's source first, rather
than building the missing behavior into the consuming page.

## Example

```tsx

```
