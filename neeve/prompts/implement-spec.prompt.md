---
description: Implement a frozen spec task — context-first, reuse-first, typed contracts, behaviour tests
---
Use the `implement-spec` skill. Require an approved spec (or a specific task
within one) before writing code — if none is given, ask which spec/task to
implement rather than guessing scope. All 7 quality gates must pass before the
work is considered done; do not mark it complete otherwise. Completion is not
reported without the skill's Production Consequence and Residual Risks block —
state blast radius/rollback and any gap explicitly, or "none identified,
verified via [what was checked]."
